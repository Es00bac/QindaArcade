#!/bin/sh
set -eu
project_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if [ ! -x "$project_dir/build/qinda-prism-kart" ]; then
    printf '%s\n' 'Build the games first with ./build-games.' >&2
    exit 1
fi
exec "$project_dir/build/qinda-prism-kart" "$@"
